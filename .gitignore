# Data: ignore everything, keep only the sheets
data/**
!data/
!data/DATASHEET.md
!data/SURVEYSHEET.md
!data/SYNTHSHEET.md

# Model weights and checkpoints
checkpoints/
outputs/
*.safetensors
*.bin
*.pt
*.ckpt

# wandb
wandb/
.wandb/

# Secrets. WANDB_API_KEY and any API keys come from the environment.
.env
.env.*
!.env.example

# Backstop only. Internal reference material must live outside the repo
# entirely, not here: an ignore rule is one `git add -f` or one careless
# edit away from failing, and an ignored file is invisible to the diff
# that confidentiality-check reads.
/context.md
*.private.md
/private/
transcript_description*

# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.ipynb_checkpoints/

# OS / editor
.DS_Store
.idea/
.vscode/
