# transcribed-transformer

Research towards models that answer compliance questions over collections of ASR call transcripts, and a measurement of how much architecture and pretraining distribution matter for that task.

> **Status (2026-09-24): experimentation is ongoing.** Early results suggest encoder-decoder models perform better than decoder-only models on this task. Full results will be shared when they are ready; the design below includes the original plans, and unmeasured results remain `TBD`.

## TLDR — context and motivation

Compliance question answering over call transcripts is a task where reported results and deployed results tend to diverge. Models that look strong on benchmarks built from clean written text do noticeably worse once the input is genuine ASR output, and the usual response is to reach for a frontier model behind an API. That works. It is also an expensive answer to a task that ought to be tractable at a fraction of the size.

The wager here is that the cause is the input itself. **Transcribed speech is disfluent, and it is long.** Fillers, restarts, repairs and recognition errors are not noise to be cleaned away before the real work starts; they *are* the distribution. And a compliance judgement usually depends on how the whole conversation fits together, while causal attention builds each part of the transcript's representation without seeing what comes after it.

This repository exists to put numbers on that rather than assert it, and to train models that work on ASR-mangled text instead of in spite of it.

## The problem

In regulated industries, conversations between staff and customers are recorded, transcribed, and reviewed against compliance questions. The questions vary in kind and are usually written by the reviewing organisation: *did the advisor communicate clearly*, *did the customer show signs of vulnerability*, *did the customer make a complaint*, *was it handled properly*.

The task has a specific and unusual shape:

- **Input is long, but not uniformly.** A single transcript can run to tens of thousands of tokens, while a great many are far shorter. A *case* is the whole interaction with one customer: several transcripts, judged together.
- **The text is diarised ASR output.** Speaker-labelled turns, one turn per line (rendered to the model as `<line number>: <speaker>: <text>`, so evidence can be cited by line), with disfluencies, recognition errors, and machine-restored punctuation. Roles are not labelled: who is the advisor and who is the customer has to be inferred from the conversation, and calls are not reliably two-party — spouses, relatives and colleagues join advice calls, which regulatory guidance on vulnerable customers explicitly anticipates.
- **Output is short and structured.** For each question: one answer, the set of transcript line numbers that constitute evidence, and a short justification.
- **Questions are open-ended, and so are their answers.** New questions are written all the time, by different organisations. The model receives the question text, the permitted answers, and the grading rule for each one, all as input. It cannot rely on a fixed, trained-in label set: the same underlying judgement is variously expressed as `{yes, no}`, as `{pass, partial pass, fail, NA}`, or as opaque codes. A question never seen during training must work.
- **Case semantics differ per question.** Some questions pass if *any* call satisfies them; others only if *every* call does. The model has to infer which from the question.

Scope: **speech transcripts only.** Chat logs, scanned documents and emails are excluded, because the hypotheses below are specifically about ASR distribution and disfluency.

## Two hypotheses

**1. The architecture is mismatched.** Long input, short structured output, and judgements that depend on the conversation as a whole. Under causal attention the answer is generated after the whole transcript and can attend to all of it, but every transcript token is encoded without seeing anything later: the opening of a call is represented without knowing how the call ends. A question like "was this complaint handled properly" depends on how early and late turns relate, so bidirectional attention, where every transcript token is encoded in light of the whole call, should be the better inductive bias.

**2. The pretraining distribution is mismatched.** Models are pretrained on written text. The input here is disfluent transcribed speech, where fillers, repairs and repetition carry real signal, and recognition errors are systematic rather than random. Pretraining on transcript-like text should help.

## Design — the experiments

Six trained designs were considered and one frontier API baseline. The original arms come from one model family, Qwen3, so that an arm differs from the control in exactly one thing: the attention mask, or the architecture built from the same weights. The native encoder-decoders (E4), the hybrids (E6) and the stitched designs (E4-mix-and-match, E5) come from the Gemma 3 and T5Gemma 2 family, each with a decoder-only control from the same family under the same recipe, and three cross-family decoders serve as matched controls. Every arm runs at about 1.7B first (full fine-tuning fits one GPU), then larger sizes for the arms that survive; same data, same split, same `<n>: <role>: <text>` rendering, loss on the answer only with zero prompt loss, a 32k sequence length for the Qwen arms and a 16k cap for the Gemma-family arms. Verdicts come from a literature review (2026-09-10); results are `TBD` until a run produces them and are recorded per run in `experiments/`.

| # | Design | Models | Status |
|---|---|---|---|
| E0 | **API baseline.** Frontier model, same prompt and rendering, prompt caching, structured output | Claude Sonnet 5 (primary), Claude Opus 5 (ceiling), one non-Anthropic model (TBD) | Started; stopped on 2026-09-22 at 28,892 of 38,220 requests, resumable, not yet scored. Sonnet labelled the family questions and Opus adjudicated the benchmark, so both partly grade their own work; the non-Anthropic model is the uncontaminated point. |
| E1 | **Causal SFT.** Standard decoder fine-tuning, causal attention, loss on the answer only. A "+" marks an adaptation stage on transcript text before the fine-tune, "++" one with more data (E1+, and E2+ / E2++ under E2) | Qwen3-1.7B-Base (full FT, the original control); the Gemma 3 decoders the native E4 encoder-decoders were adapted from, Gemma 3 270M, 1B PT and 4B PT (full FT, the E4 pairs' controls); cross-family matched controls Qwen2.5-1.5B, Hunyuan-1.8B and SmolLM3-3B-Base (full FT); E1+ = Qwen3-1.7B after a causal adaptation stage on the training transcripts | Run. Clean macro-F1 / unseen-question macro-F1: Qwen3-1.7B 0.735 / 0.654; Gemma 3 270M 0.446 / 0.315, 1B 0.651 / 0.496, 4B 0.778 / 0.702 (the first decoder in its family to transfer to unseen questions above chance); Qwen2.5-1.5B 0.702 / 0.574; Hunyuan-1.8B 0.729 / 0.623; SmolLM3-3B 0.774 / 0.698; E1+ 0.738 / 0.650, within 0.005 of E1. |
| E2 | **Prefix-LM SFT.** Same as E1 with bidirectional attention over the prompt, causal over the answer | The Qwen3 E1 checkpoints (base, + and ++), same everything, mask only | Run. The cleanest test of hypothesis 1: a causal-pretrained decoder given bidirectional input attention at fine-tuning gained 2.3 to 6.4 points at 1B to 8B and beat a full encoder-decoder (arXiv 2510.26622). Needs FlexAttention (FlashAttention-2 cannot express the mask) and gives up KV sharing across the questions asked of one call. |
| E3 | **Encoder-decoder built by our own adaptation of a decoder-only model.** The base decoder's layers as a bidirectional encoder, new cross-attention in the decoder, an adaptation stage on transcript text, then the fine-tune with the decoder reading only the encoder | Qwen3-1.7B-Base by the T5Gemma recipe: encoder and decoder both from the same weights, cross-attention initialised from self-attention, 661 M adaptation tokens (`src/train/encdec.py`) | Run (2026-09-15): trailed E1 by 0.35 in validation loss from the start and closed at E1's rate, stopped at step 620 without a benchmark. The adaptation converged; the transfer did not. A budget result (661 M tokens against the ~2 T behind a native model), not a verdict on the idea. |
| E4 | **Native encoder-decoder.** T5Gemma 2 fine-tuned as shipped; the decoder reads the encoder through its own cross-attention. **E4-mix-and-match**: one native encoder with a different native decoder, joined by a regression-fitted affine stitch on the encoder output (`src/train/stitched.py`) | T5Gemma 2 270m-270m and 1b-1b, each against the Gemma 3 decoder it was adapted from under the E1 recipe; mix-and-match at 1B/270M both ways and 4B/270M both ways (the 4B arms under FSDP); a 4B encoder + 1B decoder arm is configured and waits on a hardware route (about 1 GB per card over what fits here without freezing the embedding tables); 4b-4b on rented hardware | Run. Pairs (2026-09-16), clean macro-F1 / unseen-question macro-F1: 1b-1b 0.744 / 0.677 against Gemma 3 1B 0.651 / 0.496; 270m-270m 0.660 / 0.509 against Gemma 3 270M 0.446 / 0.315; confound accepted, architecture plus Google's adaptation tokens. Mix-and-match (2026-09-24/25): 270M encoder + 1B decoder 0.673 / 0.530; 1B encoder + 270M decoder 0.724 / 0.646; 4B encoder + 270M decoder 0.774 / 0.694, level with the Gemma 3 4B decoder (0.778 / 0.702) on answers and evidence overall. At equal parameter count the capacity is worth far more in the encoder and zero-shot question transfer travels with it; the 270M encoder + 4B decoder arm is running. One seed per cell. |
| E5 | **Encoder stitched into a different decoder's embedding space.** A native encoder reads the prompt; a regression-fitted stitch maps each of its states into the embedding space of an unrelated decoder-only model, which reads them as input positions through its own self-attention, then the answer. No cross-attention, and the decoder is E1's exactly: the same-decoder test of an encoder in front | T5Gemma 2 270m encoder into Gemma 3 1B, control `e1-gemma3-1b-pt` | Planned. About 1.3× E1's compute per step: a fair test, not a cheaper model. |
| E6 | **Hybrid: the decoder reads both the encoder's output and the raw prompt.** Native hybrid: T5Gemma 2 1b-1b with the decoder fed prompt then answer (its merged attention covers both paths, no new weights). Decoder-only + encoder: Gemma 3 1B fed the prompt as in E1 plus the T5Gemma 2 1b-1b encoder through new cross-attention gated shut at start (`src/train/hybrid.py`), and a frozen-decoder variant of it | Native hybrid against the 1b-1b; decoder-only + encoder against `e1-gemma3-1b-pt`, identical to it at step 0; the frozen-decoder variant built and held | Run. Native hybrid (2026-09-21): lands on the 1b-1b's level (0.740 vs 0.744 clean macro-F1, evidence F1 0.618 vs 0.619) at half the throughput and 0.02 below on unseen questions; the raw prompt adds nothing the encoder's states lack, and the decoder does not fall back to its own level. Decoder-only + encoder (2026-09-24): the gate never opened (about 1% for the whole run), validation identical to the control, benchmark 0.01 below it; a free decoder has no local incentive to read an encoder within a task fine-tune. Closest prior work: BERT-fused NMT, CALM, NVLM-H. |

Order: E0 and E1 at 1.7B (which also settle the training stack and the evaluation pipeline); E2 against E1; E3 if E2 moved the metric; the E4 pairs; the E6 hybrids; E4-mix-and-match; E5 for the same-decoder question; scale the survivors; then hypothesis 2, E1 and E2 repeated from a checkpoint continued-pretrained on transcript text. Training uses every corpus on disk; the licence tracks decide what is released, not what a model sees. The data, its licence tracks and its provenance are recorded in [`data/DATASHEET.md`](data/DATASHEET.md), with the corpus survey in [`data/SURVEYSHEET.md`](data/SURVEYSHEET.md) and the transcript-synthesis recipes in [`data/SYNTHSHEET.md`](data/SYNTHSHEET.md).

Every arm is scored on the same benchmark: cases built from public call corpora, questions with explicit answer options and grading rules, labels produced by LLM labelling of real, unmodified calls (a labeller answers each question with evidence first; a separate LLM-as-a-judge step assesses label quality), each call in a clean and a real-ASR variant so the cost of transcription noise is measured directly. The generator and its data format are documented in [`src/synthesis/README.md`](src/synthesis/README.md) and the DATASHEET's "Labelled data" section.

Results are recorded, never estimated; negative results get written up the same as positive ones.

This is independent research on public data. It is motivated by a class of problem common in regulated industries, and it contains no proprietary data, systems or findings from any employer. Licensed under [Apache 2.0](LICENSE).
